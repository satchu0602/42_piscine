// strncmp文字列*s1 と文字列*s2 を先頭からn文字比較します。'\0'以降の比較は行いません。
// nは、「number（数）」または「length（長さ）」を表す略語です。
// strncmp関数は、2つの文字列の最大比較長を指定するために n パラメータを使用します。
// n パラメータには、比較する文字列の最大長を指定する整数が渡されます。
// strncmp関数は、2つの文字列が指定された長さで等しいかどうかを比較します。
// したがって、n は、比較する文字列の長さを指定するために使用される略語となります。

#include <unistd.h>

