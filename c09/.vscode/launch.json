{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "save all",
            "type": "shell",
            "command": "${command:workbench.action.files.saveAll}"
        },
        {
            "label": "norminette",
            "type": "shell",
            "command": "norminette -R CheckForbiddenSourceHeader '${file}'",
            "dependsOn": "save all",
            "problemMatcher": []
        }
    ],
    "configurations": [
    
    ]
}